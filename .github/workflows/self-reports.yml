name: Self reports update

on:
  schedule:
    # GitHub 內建排程保留當備援
    # 台灣平日 22:00
    - cron: "0 14 * * 1-5"

    # 次日早上 08:00
    # 週二～週六
    - cron: "0 0 * * 2-6"

  # Cloudflare Cron 主排程
  repository_dispatch:
    types: [self_reports]

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: self-reports
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Update self reports
        env:
          PUSHOVER_APP_TOKEN: ${{ secrets.PUSHOVER_APP_TOKEN }}
          PUSHOVER_USER_KEY: ${{ secrets.PUSHOVER_USER_KEY }}
        run: python scripts/update_self_reports.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/self_reports.json data/self_reports_history.json data/self_reports_sent.json

          if git diff --cached --quiet; then
            echo "No data changes"
            exit 0
          fi

          git commit -m "data: self reports update"

          git pull --rebase

          git push
